let raio = 1;

let areaCirculo = Math.PI * raio * raio;
let comprimentoCirculo = 2 * Math.PI * raio;


console.log(`A área do circulo é de ${areaCirculo.toFixed(2)} e seu comprimento é de ${comprimentoCirculo.toFixed(1)}.`);